# دليل اختبار «مُحاضِر»

## اختبارات آلية

ينفذ الأمر `pnpm check` فحص TypeScript، بينما ينفذ `pnpm test` اختبارات قواعد المجال. تركز الاختبارات الحالية على المنطق غير المرتبط ببيئة الهاتف: إنشاء سنة نشطة، منع تكرار الترم، والتحقق من أقسام المادة النظرية والعملية.

| الأمر | الغرض | الحالة عند التسليم |
|---|---|---|
| `pnpm check` | التحقق من أنواع التطبيق والخادم والمسارات | ناجح |
| `pnpm test` | اختبار قواعد الهيكل الدراسي | 4 اختبارات ناجحة؛ اختبار مصادقة القالب متخطى |

## اختبار يدوي على الهاتف

يتطلب اختبار الميكروفون والتحويل إلى نص هاتفاً حقيقياً. افتح المشروع في Expo Go ثم أنشئ سنة وترماً ومادة عملية. اختر المادة والقسم العملي، وسجّل بضع ثوانٍ، واحفظ المحاضرة، ثم افتحها. ينبغي أن يظهر زر «تحويل إلى نص»، ويتغير إلى حالة معالجة ثم نص عند نجاح الخدمة. بعد ذلك، اضغط «إنشاء ملخص» وتحقق من ظهور النظرة العامة والنقاط والمصطلحات والأسئلة.

> لا تختبر تحويل تسجيلات خاصة أو حساسة في بيئة تطوير مشتركة. استخدم تسجيلاً تجريبياً قصيراً وتحقق من نصه قبل الاعتماد عليه أكاديمياً.

## حالات ينبغي تغطيتها لاحقاً

ينبغي أن يتضمن الإصدار التالي اختبارات محاكاة لأذونات الميكروفون، وإدارة فشل الشبكة، وتجاوز حجم الملف، وتشغيل الصوت، وحفظ ملف التسجيل في مساحة المستندات. كما يلزم اختبار تكامل لمسار رفع الصوت إلى النص ونتيجة التلخيص المنظمة على خادم الاختبار.
