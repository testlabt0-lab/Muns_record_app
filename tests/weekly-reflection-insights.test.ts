import { describe, expect, it } from "vitest";
import { getWeeklyReflectionInsights } from "../lib/weekly-reflection-insights";

describe("مؤشرات السجل الانعكاسي", () => { it("يحسب متوسط التقييم ونقطة التركيز الأبرز والاتجاه", () => { const insights = getWeeklyReflectionInsights([{ weekStart: "2026-01-05", note: "أ", rating: 3, focusAreas: ["focus"], followUpGoal: "خطوة", followUpPriority: "high", followUpCompleted: true, updatedAt: "x" }, { weekStart: "2026-01-12", note: "ب", rating: 5, focusAreas: ["focus", "review"], followUpGoal: "خطوة ثانية", followUpPriority: "low", updatedAt: "x" }]); expect(insights.averageRating).toBe(4); expect(insights.leadingFocus).toBe("focus"); expect(insights.followUpCompletionPercent).toBe(50); expect(insights.followUpPriorityBreakdown.find((item) => item.priority === "high")).toMatchObject({ total: 1, completed: 1, percent: 100 }); expect(insights.trend).toHaveLength(2); }); });
