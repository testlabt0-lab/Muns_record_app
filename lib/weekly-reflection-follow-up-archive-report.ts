import type { WeeklyReflection } from "./study-types";
import type { getFollowUpCompletionInsights } from "./weekly-reflection-follow-up-insights";

function escapeHtml(value: string) { return value.replace(/[&<>'"]/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", '"': "&quot;" })[character] ?? character); }
export function createFollowUpArchiveReportHtml(reflections: WeeklyReflection[], insights: ReturnType<typeof getFollowUpCompletionInsights>, getSubjectName: (subjectId?: string) => string | undefined, generatedAt = new Date()) { const rows = reflections.length ? reflections.map((reflection) => { const priority = reflection.followUpPriority === "high" ? "عالية" : reflection.followUpPriority === "low" ? "منخفضة" : "متوسطة"; const meta = [priority ? `أولوية ${priority}` : "", getSubjectName(reflection.followUpSubjectId) ?? ""].filter(Boolean).join(" · "); return `<article><h2>${new Date(reflection.weekStart).toLocaleDateString("ar", { year: "numeric", month: "long", day: "numeric" })}</h2><p class="meta">${escapeHtml(meta)}</p><p>${escapeHtml(reflection.followUpGoal ?? "")}</p></article>`; }).join("") : "<p class=\"empty\">لا توجد خطوات مكتملة ضمن البحث الحالي.</p>"; return `<!DOCTYPE html><html dir="rtl" lang="ar"><head><meta charset="utf-8"/><style>@page{margin:26px}body{color:#172554;direction:rtl;font-family:Arial,sans-serif}h1{color:#047857;margin:0 0 6px}.muted{color:#64748B;font-size:12px}.metrics{display:flex;gap:8px;margin:14px 0}.metric{background:#ECFDF5;border:1px solid #A7F3D0;border-radius:10px;flex:1;padding:9px}.metric b{color:#047857;font-size:18px}.metric span{color:#475569;display:block;font-size:10px;margin-top:3px}article{background:#F0FDF4;border:1px solid #BBF7D0;border-radius:12px;margin:12px 0;padding:12px}h2{color:#047857;font-size:14px;margin:0 0 7px}p{color:#334155;font-size:13px;line-height:1.7;margin:0}.meta{color:#047857;font-size:11px;font-weight:bold;margin-bottom:7px}.empty{color:#64748B}.footer{color:#64748B;font-size:11px;margin-top:22px}</style></head><body><h1>أرشيف خطوات المتابعة</h1><p class="muted">${reflections.length} خطوة مكتملة · أُنشئ محلياً في ${escapeHtml(generatedAt.toLocaleString("ar"))}</p><section class="metrics"><div class="metric"><b>${insights.currentWeeklyStreak}</b><span>سلسلة أخيرة</span></div><div class="metric"><b>${insights.bestWeeklyStreak}</b><span>أفضل سلسلة</span></div><div class="metric"><b>${insights.completedCount}</b><span>إجمالي المكتمل</span></div></section>${rows}<p class="footer">يبقى هذا التقرير محلياً حتى تختار مشاركته من جهازك.</p></body></html>`; }
