import type { Subject } from "./study-types";
import type { ArchivedWeeklyGoal } from "./subject-weekly-goal-archive";

function escapeHtml(value: string) { return value.replace(/[&<>'"]/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", "\"": "&quot;" })[char] ?? char); }
export function createSubjectWeeklyGoalArchiveReportHtml(subject: Subject, goals: ArchivedWeeklyGoal[]) { const rows = goals.length ? goals.map((entry) => `<tr><td>${new Date(entry.goal.weekStart).toLocaleDateString("ar", { year: "numeric", month: "long", day: "numeric" })}</td><td>${entry.goal.reviewTarget}</td><td>${entry.reviewedPercent}%</td><td>${entry.goal.focusMinutesTarget} د</td><td>${entry.focusPercent}%</td></tr>`).join("") : "<tr><td colspan=5>لا توجد أهداف أسبوعية مكتملة بعد.</td></tr>"; return `<!doctype html><html dir="rtl"><head><meta charset="utf-8"><style>body{font-family:Arial,sans-serif;color:#172554;padding:28px}h1{font-size:24px}p{color:#475569}table{border-collapse:collapse;width:100%;margin-top:20px}th,td{border:1px solid #cbd5e1;padding:10px;text-align:right}th{background:#eff6ff}footer{margin-top:24px;color:#64748b;font-size:11px}</style></head><body><h1>أرشيف أهداف الأسابيع</h1><p>المادة: <b>${escapeHtml(subject.title)}</b></p><table><thead><tr><th>بداية الأسبوع</th><th>هدف البطاقات</th><th>المنجز</th><th>هدف التركيز</th><th>المنجز</th></tr></thead><tbody>${rows}</tbody></table><footer>تقرير محلي من تطبيق مُحاضِر · ${new Date().toLocaleDateString("ar")}</footer></body></html>`; }
