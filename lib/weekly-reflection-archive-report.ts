import type { WeeklyReflection } from "./study-types";

function escapeHtml(value: string) { return value.replace(/[&<>'"]/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", '"': "&quot;" })[character] ?? character); }
export function createWeeklyReflectionArchiveReportHtml(reflections: WeeklyReflection[], generatedAt = new Date()) { const rows = reflections.length ? reflections.map((reflection) => `<article><h2>${new Date(reflection.weekStart).toLocaleDateString("ar", { year: "numeric", month: "long", day: "numeric" })}</h2><p>${escapeHtml(reflection.note).replace(/\n/g, "<br/>")}</p></article>`).join("") : "<p class=\"empty\">لا توجد ملاحظات ضمن الفترة المختارة.</p>"; return `<!DOCTYPE html><html dir="rtl" lang="ar"><head><meta charset="utf-8"/><style>@page{margin:26px}body{color:#172554;direction:rtl;font-family:Arial,sans-serif}h1{color:#5B21B6;margin:0 0 6px}.muted{color:#64748B;font-size:12px}article{background:#FAF5FF;border:1px solid #E9D5FF;border-radius:12px;margin:12px 0;padding:12px}h2{color:#6D28D9;font-size:14px;margin:0 0 7px}p{color:#334155;font-size:13px;line-height:1.7;margin:0}.empty{color:#64748B}.footer{color:#64748B;font-size:11px;margin-top:22px}</style></head><body><h1>أرشيف الملاحظات الأسبوعية</h1><p class="muted">${reflections.length} ملاحظة مكتوبة · أُنشئ محلياً في ${escapeHtml(generatedAt.toLocaleString("ar"))}</p>${rows}<p class="footer">يبقى هذا التقرير محلياً حتى تختار مشاركته من جهازك.</p></body></html>`; }
